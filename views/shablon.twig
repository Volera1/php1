<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>{{ title }}</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
		<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css" rel="stylesheet"/>
	</head>
	<body>

		<nav class="navbar navbar-expand-sm bg-light">
			<div class="container-fluid">
				<a class="navbar-brand" href="#">
					<i class="fa-solid fa-circle-radiation fa-2x fa-spin"></i>
				</a>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNav">
					<ul class="navbar-nav">
						<li class="nav-item">
							<a class="nav-link {{ title == " главная" ? 'active' }}" aria-current="page" href="/">Главная</a>
						</li>
						{% for type in types %}
							<!--так как item это словарик с двумя ключами, используем их для вывода ссылки -->
							<li class="nav-item">
								<a class="nav-link {{ title == type.name|title ? 'active' }}" aria-current="page" href="/?type={{type.name}}">{{ type.name|title }}</a>
							</li>
						{% endfor %}
						<li class="nav-item">
							<a class="nav-link {{ title == " поиск" ? 'active' }}" aria-current="page" href="/search">Поиск</a>
						</li>
						<li class="nav-item">
							<a class="nav-link {{ title == " добавить" ? 'active' }}" aria-current="page" href="/char/create">Добавить</a>
						</li>
					</li>
					<li class="nav-item">
						<a class="nav-link {{ title == " добавитьвселенную" ? 'active' }}" aria-current="page" href="/type/create">ДобавитьВселенную</a>
					</li>
				</ul>
			</div>
		</div>
	{# </body>
</html> #}</nav>
{% block content %}
пустота{% endblock %}
<div class="row-3">Сообщение из сессии: {{my_session_message}}
<br>
Последние сообщения: <br>
{% for m in messages %}
	<a href="{{m}}">{{m}}</a><br>
{% endfor %}
<hr></div>

</body></html>
